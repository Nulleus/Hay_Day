using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockObject : MonoBehaviour
{
    public bool BlockObjectStatus;

    public bool GetBlockObjectStatus()
    {
        return BlockObjectStatus;
    }
    public void SetBlockObjectStatus(bool status)
    {
        BlockObjectStatus = status;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
